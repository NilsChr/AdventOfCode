{
  "name": "advent-of-code",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "FILE=input bun run index.ts",
    "test": "FILE=test bun run index.ts",
    "generate": "bun run generate.ts",
    "help": "bun run start --help",
    "debug": "bun run index.ts"
  },
  "devDependencies": {
    "bun-types": "^1.3.3"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "ora": "^8.1.1"
  }
}