{
  "name": "advent-of-code",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "FILE=input bun run index.ts",
    "test": "FILE=test bun run index.ts",
    "generate": "bun run generate.ts"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "ora": "^8.1.1"
  }
}